{
  "name": "piscine-blockchain",
  "version": "1.0.0",
  "description": "Blockchain project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
